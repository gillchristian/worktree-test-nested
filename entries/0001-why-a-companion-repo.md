# Why a companion repo

Notes about code that is not on the parent's trunk yet cannot live on the
parent's trunk either — they would describe something nobody can read. So they
live on a branch of the same name, beside the branch they describe, and move to
`main` when the code lands.
